# Força o Nixpacks a usar apenas o provedor de Node.js e ignorar detecção de Deno
providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.build]
cmds = ["npm install", "npm run build"]

[start]
cmd = "npm run preview -- --host"
